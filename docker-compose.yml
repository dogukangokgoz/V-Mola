version: '3.8'

services:
  mola-takip:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
    networks:
      - mola-network

networks:
  mola-network:
    driver: bridge